service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false; // locked any operations on database, only Admin SDK is still possible.
    }
  }
}
